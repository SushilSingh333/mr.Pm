// Payload admin import map. Regenerate after adding custom admin components with
// `pnpm --filter @mpm/cms exec payload generate:importmap`. Typed so the workspace
// typechecks; Payload overwrites this file when it regenerates.
export const importMap: Record<string, unknown> = {};
